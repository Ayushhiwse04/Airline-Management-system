document.addEventListener("DOMContentLoaded", () => {
  const train = ["Rose Gold", "Maharani"];
  const destination = ["Mumbai", "Pune"];
  let from = "";
  let to = "";
  let trainDetails = localStorage.getItem(trainDetails);
});
